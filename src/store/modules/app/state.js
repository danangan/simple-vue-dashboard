const state = {
  // global Notification
  globalNotificationStatus: false,
  // the global notification type could be 'error', 'success', or empty string
  globalNotificationType: '',
  globalNotificationMessage: '',
  // global overlay loader
  globalLoaderStatus: false,
  globalLoaderMessage: '',
  appRender: false,
};

export default state;
